

# 移动魔方 前端测试点

- [站点](#站点)
  - [开发人员](#开发人员)
  - [发布版本](#发布版本)
  - [接口管理](#接口管理)
  - [资源管理](#资源管理)
  - [组件管理](#组件管理)
  - [审计历史](#审计历史)
  - [页面模板](#页面模板)
    - [组件编辑](#组件编辑)
    - [逻辑编辑](#逻辑编辑)
  - [页面](#页面)
    - [组件编辑](#组件编辑-1)
    - [逻辑编辑](#逻辑编辑-1)
- [自定义组件](#自定义组件)
  - [开发人员](#开发人员)
  - [关联站点](#关联站点)
  - [申请记录](#申请记录)
- [模板组件](#模板组件)
  - [开发人员](#开发人员-1)
  - [关联站点](#关联站点-1)
  - [接口管理](#接口管理-1)
  - [资源管理](#资源管理-1)
  - [组件管理](#组件管理-1)
  - [申请记录](#申请记录-1)
  - [组件编辑](#组件编辑-2)
  - [逻辑编辑](#逻辑编辑-2)

## 站点

* **站点**列表展示。
* **站点**新建。
* **站点**编辑。
  字段：站点名 (必填)，关键字，描述。
* **站点**删除。
* **站点**上线。
* **站点**下线。
* **站点**发布。
  字段：版本（必填），描述。会添加一条版本数据。
* **站点**页面 重置。
  数据来源最近5条**草稿**数据，最近5条**版本**数据。

### 开发人员
* **合作者**列表展示。
* **合作者**添加。
* **合作者**删除。
* **合作者**转为**管理者**。
* **管理者**列表展示。
* **管理者**添加。
* **管理者**删除。

### 发布版本
* **版本**列表展示。
* **版本**回滚。

### 接口管理
* **接口**列表展示。
  支持**关键字**搜索。
* **接口**环境配置。
  预览时可以通过切换环境，调用不同环境配置的接口。
* **接口**新建。
  字段：名称（必填），方式（必填），地址（必填），不同环境下地址，入参，出参。
* **接口**编辑。
  字段：名称（必填），方式（必填），地址（必填），不同环境下地址，入参，出参。
* **接口**删除。
* **接口**复制。

### 资源管理
* **资源**列表展示。
  支持**关键字**搜索。
* **资源**上传。

### 组件管理
* **自定义组件**列表展示。
  支持**关键字**搜索。
* **自定义组件**申请。
* **模板组件**列表展示。
  支持**关键字**搜索。
* **模板组件**申请。

### 审计历史
* **审计**列表展示。

### 页面模板
* **页面模板**列表展示。
* **页面模板**新建。
  字段：名称（必填），页面模板。
* **页面模板**编辑。
  字段：名称（必填）。
* **页面模板**删除。
* **页面模板**预览。
* **页面模板**保存。

#### 组件编辑
* **组件**拖拽布局。
  数据来源：**内置组件**，已授权自定义组件，已授权模板组件。
* **组件**属性编辑。
  **属性**，**样式**，**校验**，**别名**。
  部分**组件**没有可编辑**属性**或者没有可编辑**样式**。
  **校验**仅针对可编辑的**属性**，在**逻辑**中读取。
  **别名**可在**逻辑**与**节点地图**中展示，便于选择**组件**。
* **组件**节点地图。
  节点地图 可隐藏组件，仅编辑使用，对正式页面展示无效。
* **组件**编辑撤销。
* **组件**编辑重做。

#### 逻辑编辑
* 事件。
  - 当页面初始化
  - 当修改**组件**-**属性**
  - 部分**组件**会自带事件。
* 控制。
  - 如果。
  - 如果那么。
  - 结束进程。
* 命令。
  - 赋值浏览器缓存。
  - 赋值页面缓存。
  - 赋值**组件**-**属性**。
  - 页面滚动至**某Box组件**。
  - 显示**组件**。
  - 隐藏**组件**。
  - 提示信息。
* 特殊操作。
  - 数据提取。
  - 页面跳转。
  - 数据渲染。
  - 判断是否为身份证号码。
  - 判断是否符合输入正则表达式。
  - 信息校验，信息校验中可选择**校验**的编辑结果。
* 本地数据。
  - 读取浏览器缓存。
  - 读取页面缓存。
  - 读取链接参数缓存，基于query。
  - 读取**组件**-**属性**。
* 接口数据。
  - 发起**接口**请求。数据来源：**站点**的**接口**列表
  - 获得**接口**返回数据。数据来源：**站点**的**接口**列表
  - 绑定请求头。
  - 绑定入参。
  - 重命名出参。
* 运算。
  - 数据计算与判断。
* 集合。
  - 预设任务。
  - 执行任务。

## 页面
* 页面 列表展示。
* 页面 新建。
  字段：名称（必填），路径（必填），页面模板。
* 页面 编辑。
  字段：名称（必填），路径（必填）。
* 页面 设为主页。
* 页面 删除。
* 页面 预览。
* 页面 保存。
  会添加一条**草稿**数据，重置使用。

#### 组件编辑
* **组件**拖拽布局。
  数据来源：**内置组件**，已授权**自定义组件**，已授权**模板组件**。
* **组件**属性编辑。
  **属性**，**样式**，**校验**，**别名**。
  部分**组件**没有可编辑**属性**或者没有可编辑**样式**。
  **校验**仅针对可编辑的**属性**，在逻辑中**读取**。
  **别名**可在**逻辑**与**节点地图**中展示，便于选择**组件**。
* **组件**节点地图。
  **节点地图**可隐藏**组件**，仅编辑使用，对正式页面展示无效。
* **组件**编辑撤销。
* **组件**编辑重做。

#### 逻辑编辑
* 事件。
  - 当页面初始化
  - 当修改**组件**-**属性**
  - 部分**组件**会自带事件。
* 控制。
  - 如果。
  - 如果那么。
  - 结束进程。
* 命令。
  - 赋值浏览器缓存。
  - 赋值页面缓存。
  - 赋值**组件**-**属性**。
  - 页面滚动至**某Box组件**。
  - 显示**组件**。
  - 隐藏**组件**。
  - 提示信息。
* 特殊操作。
  - 数据提取。
  - 页面跳转。
  - 数据渲染。
  - 判断是否为身份证号码。
  - 判断是否符合输入正则表达式。
  - 信息校验，信息校验中可选择**校验**的编辑结果。
* 本地数据。
  - 读取浏览器缓存。
  - 读取页面缓存。
  - 读取链接参数缓存，基于query。
  - 读取**组件**-**属性**。
* 接口数据。
  - 发起**接口**请求。数据来源：**站点**的**接口**列表
  - 获得**接口**返回数据。数据来源：**站点**的**接口**列表
  - 绑定请求头。
  - 绑定入参。
  - 重命名出参。
* 运算。
  - 数据计算与判断。
* 集合。
  - 预设任务。
  - 执行任务。

##**自定义组件**

### 基础操作
* **自定义组件**列表展示。
* **自定义组件**上传创建。
* **自定义组件**上传更新。
* **自定义组件**编辑。
  字段：组件名（必填），描述。
* **自定义组件**发布。
  字段：组件参数。

### 关联站点
* **关联站点**列表展示。
  支持**关键字**搜索。
* **关联站点**添加。

### 申请记录
* **站点申请**列表展示。
  支持**状态**搜索。
* **站点申请**同意。
* **站点申请**拒绝。
* **模板组件申请**列表展示。
  支持**状态**搜索。
* **模板组件申请**同意。
* **模板组件申请**拒绝。

##**模板组件**
* **模板组件**列表展示。
* **模板组件**新建。
  字段：名称（必填）。
* **模板组件**编辑。
  字段：名称（必填），描述。
* **模板组件**参数设置。
* **模板组件**重置。
  数据来源：最近5条**草稿**数据，最近5条**版本**数据。

### 开发人员
* **合作者**列表展示。
* **合作者**添加。
* **合作者**删除。
* **合作者**转为**管理者**。
* **管理者**列表展示。
* **管理者**添加。
* **管理者**删除。

### 关联站点
* **关联站点**列表展示。
  支持**关键字**搜索。
* **关联站点**添加。

### 接口管理
* **接口**列表展示。
  支持**关键字**搜索。
* **接口**新建。
  字段：名称（必填），方式（必填），地址（必填），入参，出参。
* **接口**编辑。
  字段：名称（必填），方式（必填），地址（必填），入参，出参。
* **接口**删除。
* **接口**复制。

### 资源管理
* **资源**列表展示。
  支持**关键字**搜索。
* **资源**上传。

### 组件管理
* **自定义组件**列表展示。
  支持**关键字**搜索。
* **自定义组件申请**。

### 申请记录
* **站点申请**列表展示。
  支持**状态**搜索。
* **站点申请**同意。
* **站点申请**拒绝。

### 组件编辑
* **组件**拖拽布局。
  数据来源：**内置组件**，已授权**自定义组件**。
* **组件**属性编辑。
  **属性**，**样式**，**校验**，**别名**。
  部分**组件**没有可编辑**属性**或者没有可编辑**样式**。
  **校验**仅针对可编辑的**属性**，在**逻辑**中读取。
  **别名**可在**逻辑**与**节点地图**中展示，便于选择**组件**。
* **组件**节点地图。
  **节点地图**可隐藏组件，仅编辑使用，对正式页面展示无效。
* **组件**编辑撤销。
* **组件**编辑重做。

### 逻辑编辑
* 参数。
  - 某参数被修改。
  - 渲染至**组件**下。
  - 触发事件。
  - 赋值**属性**。
  - 读取**属性**。
* 事件。
  - 当页面初始化
  - 当修改**组件**-**属性**
  - 部分**组件**会自带事件。
* 控制。
  - 如果。
  - 如果那么。
  - 结束进程。
* 命令。
  - 赋值浏览器缓存。
  - 赋值页面缓存。
  - 赋值**组件**-**属性**。
  - 页面滚动至**某Box组件**。
  - 显示**组件**。
  - 隐藏**组件**。
  - 提示信息。
* 特殊操作。
  - 数据提取。
  - 页面跳转。
  - 数据渲染。
  - 判断是否为身份证号码。
  - 判断是否符合输入正则表达式。
  - 信息校验，信息校验中可选择**校验**的编辑结果。
* 本地数据。
  - 读取浏览器缓存。
  - 读取页面缓存。
  - 读取链接参数缓存，基于query。
  - 读取**组件**-**属性**。
* 接口数据。
  - 发起**接口**请求。数据来源：**站点**的**接口**列表
  - 获得**接口**返回数据。数据来源：**站点**的**接口**列表
  - 绑定请求头。
  - 绑定入参。
  - 重命名出参。
* 运算。
  - 数据计算与判断。
* 集合。
  - 预设任务。
  - 执行任务。
